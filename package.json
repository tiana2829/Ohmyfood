{
  "name": "oh-myfood",
  "version": "1.0.0",
  "description": "Ohmyfood",
  "main": "index.js",
  "scripts": {
    "build:sass": "sass --no-source-map ./sass/style.scss:./assets/css/style.css",
    "watch:sass": "sass --no-source-map --watch ./sass/style.scss:./assets/css/style.css",
    "watch": "npm-run-all --parallel watch:*",
    "start": "npm-run-all --parallel watch",
    "build": "npm-run-all build:*"
  },
  "keywords": [
    "javascript",
    "node",
    "js"
  ],
  "author": "Mamitiana",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.77.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
